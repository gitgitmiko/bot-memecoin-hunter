#!/usr/bin/env node

/**
 * Health check script for Analyzer Service
 * Returns exit code 0 if healthy, 1 if unhealthy
 */

// Simple health check - can be extended to check database/redis connections
process.exit(0);

